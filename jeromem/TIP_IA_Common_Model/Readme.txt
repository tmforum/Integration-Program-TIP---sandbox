This folder consists of the TIP Tigerstripe model with all common data and
operational definitions used by the TIP Interfaces.

To Do List:
- Naming:
  - Confirm use of EntityIdentifier dialect
  - Confirm use of EntityIdentifierList
  (no need to define a data type for a simple list/array)
  - Confirm use of NameValuePairList (same as above).